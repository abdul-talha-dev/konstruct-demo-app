apiVersion: v1
kind: Service
metadata:
  name: {{ include "konstruct-demo-app.fullname" . }}
  labels:
    {{- include "konstruct-demo-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "konstruct-demo-app.selectorLabels" . | nindent 4 }}
